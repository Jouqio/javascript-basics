// ============================================================
// utils/validasi.mjs
// ============================================================

export function validasiEmail(email) {
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
}
