// ============================================================
// pesanan-benar.mjs
// Versi yang sudah diperbaiki - hanya bergantung ke format.mjs
// ============================================================

import { formatRupiah } from "./format.mjs";

export function hitungTotalPesanan(namaUser) {
  return formatRupiah(150000);
}
